{
  "name": "node-scheme",
  "version": "1.0.0",
  "description": "A scheme interpreter.",
  "_comment_main:": [
    "The main entry point for the application.",
    "Specifies the file that is loaded when this package is imported."
  ],
  "main": "src/index.js",
  "_comment_scripts:": [
    "The main entry point for the application.",
    "Specifies the file that is loaded when this package is imported."
  ],
  "_comment_type:": [
    "Tell Node that .js files use import/export instead of require/module.exports."
  ],
  "type": "module",
  "scripts": {
    "_comment_start:": [
      "'start' is recognized in as part of a special npm lifecycle.",
      "So 'npm start' can trigger this script, instead of 'npm run start'"
    ],
    "start": "node src/index.js",
    "_comment_test:": [
      "--experimental-vm-modules is required for Jest to work with ES modules."
    ],
    "test": "node --experimental-vm-modules node_modules/jest/bin/jest.js"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "typescript": "^5.9.3"
  },
  "author": "Colin D. Jones",
  "license": "ISC"
}
